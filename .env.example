# Leave empty to use the local demo without sign-in. Set to your Convex deployment URL for server data.
VITE_CONVEX_URL=

# Set ADMIN_EMAIL in the Convex deployment environment before that email creates an account.
# This is a server-only setting; do not prefix it with VITE_ or put it in the frontend bundle.
